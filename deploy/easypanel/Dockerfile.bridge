# HTTP → Hermes Bridge
# Enkel FastAPI-app som proxyar till Hermes

FROM python:3.11-slim

WORKDIR /app

# Installera dependencies
RUN pip install fastapi uvicorn httpx

# Kopiera bridge-koden
COPY hermes_bridge.py .

EXPOSE 8080

# Starta bridge
CMD ["python", "hermes_bridge.py"]
